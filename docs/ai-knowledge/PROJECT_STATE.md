# 現在状態

状態: `検証済み`（記載した範囲のみ）
最終確認日: 2026-08-22
基準コミット: `2f1214d`
基準ブランチ: `main`
公開URL: https://iku190t.github.io/ais-survey-tool/

## リポジトリ構成

- GitHub公開リポジトリ: `iku190t/ais-survey-tool`
- 静的Webアプリの中心は `index.html`。
- 分離済みの補助処理は `foundation-map-gml.js`、`terrain-advanced.js`、`google-maps-links.js`、`p2p-share.js`、`pc-web-guide.js`。
- `excel-addin/` はExcelアドイン用の配信物。
- `validate-*.js` はNode.jsから直接実行する回帰検証。現時点で `package.json` による一括テスト定義はない。
- リポジトリ内のコードとGit履歴が正本。外部知能文書は仕様索引・根拠・戻し先を保持する。

## 現行で検証できた主要領域

2026-08-22に基準コミット上で次を再実行し、成功を確認した。

- 法務局地図: 手動取得、表示保持、表示・非表示、取得中キャンセル、PC/スマホの進捗、スマホだけの容量確認、レイヤー別色、範囲CAD化、SFC書出。
- 写真: 写真帳の静的要件、実行時生成、元写真の同一セッション保持、レイヤー削除、Undo、新規ドラッグ前の旧矢印除去、位置調整、ネットワーク写真読込。
- 性能・復元: 表示だけのパン・拡大・回転で完全復元JSONを作らないこと、実データ変更の軽量自動保存、元SFCを端末内へ1回保存して初回画面から前回作業を復元すること、DEMキャッシュ上限、文字・豆図の空間インデックス。
- 現在地モード: スマホ起動、終了時の状態復元、現在地モード内の背景・地形・設定・計測・手書き、ポップアップの前面表示。
- 地形: 高度解析11モード、地形UI 16ボタン、DEM1A優先取得、等高線文字の水平表示、航空写真ラスターの高解像度取得経路。
- SFC表示: `sample.sfc` を実読込し、線・ポリライン・スプライン・文字・円・円弧、部分図、配置、線幅、起動時アイコンを確認。
- Google連携: Street Viewウィンドウと写真帳QRのリンク。
- 基盤地図: 縮尺レベル2500 GML解析とSFC保持。
- PC操作: 地図ツールバー、ツールチップ、文字・写真位置のヒット、パン動作。
- スマホ方位追従: GPSとは独立し、初期状態はOFF。方位ボタンを押した時だけ方位許可と回転を開始し、追従中は方位ボタン全体を明瞭な青で表示、再押下で停止する。ホームは停止と読み込み角度復帰。追従補間は滑らかさを維持しつつ応答時定数28ms・目標追従率88%へ高速化している。
- 現在地の航空写真: 図面別に座標系が手動確定しており、図面から500m以上離れた場合は現在地座標系へ切り替え、最新航空写真を自動表示する。図面座標系が未設定なら数値距離を表示せず、その旨を通知して現在地の最新航空写真へ安全側に切り替える。端末の既定座標系は設定補助として保存するが、別地域図面の距離計算には使わない。GPS中は年代検索を繰り返さず最新写真へ固定し、現在地と別地点の画像が交互に出る状態を防ぐ。
- 公開ライセンス: `AGPL-3.0-only`。標準本文は`LICENSE`、第7条(b)の原作者表示条件は`ADDITIONAL_TERMS.md`、利用者向け表示はヘルプの「ライセンス・著作権」に分離している。改変版の製品名は固定せず、原作品名の表示を残す。

詳細なコマンドは `TEST_MATRIX.md` を参照する。

## 現在の重要な動作方針

- DEMを使う標高処理はDEM1Aを先に使い、欠損点だけDEM5Aで補う。
- 等高線CAD化では表示倍率で精度を落とさず、DEM1A優先の1m格子を基準にする。
- 表示だけの操作は復元用データ保存を行わない。レイヤー色、手書き、写真など実データ変更は保存対象。
- 前回作業の復元は、元SFCの端末内複製と編集差分を分離する。元SFCは図面ごとに1回だけIndexedDBへ保存し、通常の編集では差分だけを更新する。元JPEGはブラウザ再起動後に再接続が必要な場合がある。
- スマホの方位追従は現在地/GPSとは独立する。ファイル読込時には方位許可を要求せず、方位ボタンを押した時だけ追従を開始する。再押下で停止し、ホーム操作は追従を停止して読み込み時の角度へ戻す。
- PCの写真ドラッグ＆ドロップは、位置情報付き写真が1枚以上正常に読めた時だけ旧写真一式を置換する。写真選択ボタンは追加・統合動作を維持する。
- ブラウザの `File` は永続化できないため、同じ起動中は直接参照を保持し、再起動後に元JPEGが必要な処理では再接続が必要になり得る。

## 未解決の検証課題

以下は2026-08-22の一括確認で失敗した。ただし、失敗内容だけでは製品機能の後退と断定できない。修正作業では、まずテスト側の古い前提と実装不具合を分ける。

|状態|テスト|確認結果|
|---|---|---|
|再現済み|`validate-gps-detail-dem.js`|古い文字列形式 `ファイル名：...` を正規表現で要求して失敗。現行実装は拡張子と接頭語を除いた名前を返し、`validate-photo-album-runtime.js` では `A.jpg` → `A` を確認済み。テスト側の期待式が古い可能性が高い。|
|再現済み|`validate-background-sxf.js`|廃止済みと思われる `backgroundSxfOpenBtn` IDを要求して失敗。現行の基盤地図2500処理は `validate-foundation-map-gml.js` と `validate-foundation-map-import.js` が成功。旧テストの適用範囲を要確認。|
|再現済み|`validate-mobile-touch.js`|ソース文字列の改行を完全一致で要求して失敗。実際のタッチ動作の成否はこの失敗だけでは判断できない。|
|再現済み|`validate-raster-placement.js`|テスト用抽出環境に `flattenSxfFeatureBlocks` が無く `ReferenceError`。アプリ本体の失敗ではなく、まずテストハーネス不足を確認する。|
|再現済み|`validate-ui.js`|旧ラスター名テンプレートを完全一致で要求して失敗。`validate-contour-label-raster-quality.js` と実SFC表示は成功しているため、命名仕様と旧テストを照合する。|

この5件を「既知の失敗」として無視し続けない。関連機能へ触れる前に、現行仕様に合わせてテストを直すのか、製品を直すのかを証拠で決める。

## 既存のGit退避タグ

- `backup-before-base-map-toggle-fix-20260808` → `56266b7`
- `backup-before-google-registry-qr-20260808` → `f3132fe`
- `backup-before-registry-progress-cancel-20260808` → `30f2225`

これらは特定作業前の履歴点であり、現在全体の推奨版ではない。現在全体の基準は `2f1214d`。
