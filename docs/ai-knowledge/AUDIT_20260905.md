# 2026-09-05 総合監査と修正

追加監査8項目の修正: `e70c5b2`。標高の先行修正 `775ef60` を含む。39ケースの追加回帰と既存回帰で下表を確認した。詳細は `INC-20260905-FOLLOWUP`、`INC-20260905-ELEVATION` と正常基準 `BASE-FOLLOWUP-AUDIT-20260905` を参照。

## 追加監査8項目の最終状態

|項目|修正・確認|状態|
|---|---|---|
|1. 不正SFZで航空写真が消える|SFC解析の失敗を尊重し、画像展開も完了してから作業を置換。異常時は旧原文・画像・編集を維持|検証済み|
|2. Drogger・地番付き法務局CADのSFC保存停止|線・円・文字の予定件数を別々に検証。最終出力に文字を含み、実際の欠落は拒否|検証済み|
|3. 図面なしGPS登録を保存できない|編集レイヤーでも保存・共有を有効化。1/500のSFCへ出力し、再読込・再保存で座標・寸法を維持|検証済み|
|4. 標高表示の1cmずれ|図面2桁切り捨て、登録・CSV3桁を維持。200,001値を再検証|検証済み|
|5. 写真の取込・位置編集で系が異なる|図面の系と変換を共用し、遠隔GPSの一時系を混ぜない|検証済み|
|6. SIMA連続読込が古い結果へ戻る|最新の世代だけ確定。クリア/別SFC読込で古い処理と予約表示を取消|検証済み|
|7. SFCに重ねたSIMAの復元漏れ|SIMA原文を独立して1回保存。参照キー・表示設定を編集スナップショットへ追加し、対応作業だけ復元|検証済み|
|8. レイヤー変更直後の図面切替で保存漏れ|旧作業の保存を切替前に確定。別作業の待機保存もキー別に保持|検証済み|

- 13ケース×3表示環境（PC・Android相当・iPhone相当）の39ケース成功。全48本は46成功、旧前提2本は同じ失敗。新規失敗なし。
- SIMA原文のスナップショットごとの複製、保存済み登録値の一括変更、端末DB削除は行わない。過去に消失したデータの再生成はできない。
- モバイルはChrome模擬。実端末、実Drogger、外部CAD、メールアプリは未検証。

## 先行監査の記録

修正前: `e85aedb8c535db5a42b644b8142ceb2354a5a8ca`。保存等の修正: `7c30b42c638ed75bad667372927109c7dfc0d0f9`。地域判定の修正: `5fe131f1cc18c7d8c83f6458164bb768d80d4761`。
復元タグ（ローカルのみ）: `backup-before-audit-fixes-20260905`。

## 確認できた不具合

|項目|原因・処置|状態|
|---|---|---|
|GPS/SIMA単独作業が復元されない|SFC必須条件を除き、独立作業の元データと編集差分を保持。ページ再読込後に測点・メモを確認|検証済み|
|SFC文字レイヤー変更が復元されない|間引きハッシュを全文ハッシュへ変更。変更時のみ計算し、旧形式も読込可能。元データと差分を同一トランザクションで保存|検証済み|
|CSVの負数座標が文字扱い|数値列だけ数式対策の引用符付与から除外。点名の数式対策は維持|検証済み|
|失敗読込で元作業を消す|SFCは解析と有効図形確認後に切替。SIMAは有効座標確認後に切替|検証済み|
|横画面の通知表示でパネル末尾が切れる|通知の高さを表示領域から除く。末尾のSIMAボタンまでスクロール可能|検証済み|
|SIMA測点・境界へスナップできない|SIMA専用空間索引から表示中の点・線を候補化。非表示時は無効|検証済み|
|GPS一時座標系とハザードの系が異なる|GPS一時表示の系を優先。GPS解除後は図面の手動系に戻る|検証済み|
|円弧だけのSFCが図面なし扱い|図形判定を円弧・楕円・スプライン・点を含む全対応種別へ統一|検証済み|
|広島・岡山を4系と判断する|四国扱いの長方形を廃止。国土数値情報N03の行政区域と国土地理院の適用区域から端末内で選択。広島III・岡山V・四国IVを確認|検証済み|

地域の正式な適用区域は [国土地理院の告示](https://www.gsi.go.jp/LAW/heimencho) を参照。現在地を逆ジオコーダへ送る案は採用せず、公開データから作成した端末内判定へ置換した。新たな位置情報の外部送信はない。タグはローカルのみで、GitHubへ追加していない。

## テスト結果

### 先行修正 `5fe131f`

- 監査で確認した6件（GPS/SIMA単独復元、SFC文字レイヤー復元、地域座標系、GPS中のハザード系、CSV負数、円弧単独図面）は修正済み。先行修正の読込失敗時の保持、SIMAスナップ、横画面通知も `validate-audit-regressions.js` で再検証した。
- 全 `validate-*.js` 47本: **45成功、2失敗**。残る `validate-background-sxf.js` と `validate-ui.js` は旧UI・旧出力形式の前提で停止し、適用範囲の見直しが未完了。全テスト成功とは扱わない。
- 5本のハーネスを補修：GPS詳細のファイル名期待式、タッチの改行差、ラスター試験の依存関数不足、Droggerモジュール版、写真試験の関数定義前の待機エラー。アプリの挙動をテストへ合わせて戻す変更はしていない。
- `validate-japan-plane-zones.js`: 47都道府県庁付近、北海道・離島の例外、監査再現点を含む76地点・全19系が成功。元行政区域内の格子・島の代表点9,668地点も別途照合して不一致0。
- `validate-japan-plane-zone-runtime.js`: モバイル相当Chrome、CPUを4倍に抑制した条件で初回判定約26ms、同じ地点の1,000回判定約275ms。20回のパン・ズーム・回転で行政区域判定0回。既存図面IV系・GPS III系の一時切替と解除後IV系への復帰、元SFC文字列の保持を確認。実機の性能保証ではない。
- 配布データ約2.45MB（gzip約1.67MB）。数値を可逆圧縮し、区域と小島の座標は必要になった時だけ展開する。Python/GISライブラリは生成時だけで、アプリへ追加しない。
- 出典・加工・制限は [行政区域判定データの説明](../../data/JAPAN_PLANE_ZONES.md)。`git diff --check` 成功。

### 先行修正時の記録

- 全 `validate-*.js` 45本: 40成功、既存前提不一致の5本失敗。実SFC検証には同梱 `sample.sfc` を使用。
- 既存失敗: `validate-background-sxf.js`、`validate-gps-detail-dem.js`、`validate-mobile-touch.js`、`validate-raster-placement.js`、`validate-ui.js`。理由は `TEST_MATRIX.md` の既知失敗を参照。成功扱いにはしない。
- 新規 `validate-audit-regressions.js` は上表の修正範囲を確認。CSV点名の数式対策、SIMA非表示、旧復元ハッシュ互換、GPS解除後の系も検証。
- 最終調整後の再実行: 追加監査、前回作業復元、GPS起動、Drogger実行時、SIMA性能が成功。`git diff --check` 成功。
- 800画地・6,400点の合成SIMA: 最終実行で初回約156ms、全体約29ms、拡大約10ms。検証PC上の数値であり実機保証ではない。

## 未確認・限界

- 全端末・全図面・全操作順を網羅していない。ほかに不具合がないとは断言しない。
- 行政界付近、海上、未確定境界、元データ以後の変化では座標系候補が適切でない場合がある。既に保存された系は変更しないため、旧版の誤った自動設定が残る図面は手動修正が必要。既存形式は自動保存した系と手動系を区別していない。
- iPhone/Androidの実機操作、Droggerの実信号、メール共有先、ブラウザの強制終了直前、OSの保存領域消去、外部CADでの読込は今回未検証。
- `e70c5b2` 以後はSFCに重ねたSIMAも独立した元データ参照で復元する。旧版でSIMA元データ自体が保存されなかった作業は、元SIMAの再選択が必要。
- IndexedDBは端末内保存。ストレージ消去・容量不足・保存完了前の終了まで保証できない。元SFCと作業メモを失わないための外部保存を代替する保証ではない。
- 戻す時は作業データを保持し、修正コミットだけをrevertする。プログラムの復元は、端末の作業データを旧形式へ巻き戻す操作ではない。旧版は新しい独立ワークスペース復元形式を解釈できないため、戻す前にSFC/CSVへ書き出す。
